export const APP_NAME = "ChatSphere";

/**
 * Timeouts & limits
 */
export const TYPING_TIMEOUT_MS = 2000;
export const MESSAGE_PAGE_SIZE = 30;

/**
 * WebSocket reconnect (future-ready)
 */
export const WS_RECONNECT_DELAY = 3000;
